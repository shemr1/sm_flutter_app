import 'package:flutter/foundation.dart';


class AppointItemCounter with ChangeNotifier
{
  // int _counter = ScheduleMeApp.sharedPreferences.getStringList(ScheduleMeApp.userAppointRequestList).length-1;
  // int get count => _counter;
  //
  // Future <void> displayResults() async
  // {
  //   int _counter = ScheduleMeApp.sharedPreferences.getStringList(ScheduleMeApp.userAppointRequestList).length-1;
  //
  //   await Future.delayed(const Duration(milliseconds: 100,),(){
  //     notifyListeners();
  //   });
  // }
}